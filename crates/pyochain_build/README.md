# pyochain-build

Utilitary crate for checking documentation quality prior to releases.

It notably checks the coherence between python stubs and rust src/ layout, generate the references for the website, and check the correctness of the navigation paths in the documentation.
