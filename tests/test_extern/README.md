# External tests

Those tests are imported from external sources and are manually adapted to work with pyochain.

They come mostly from Cpython, but also from `sortedcontainers` and `sliceview` libraries.
