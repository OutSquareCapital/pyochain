"""External tests adapted from external tests suites for pyochain reimplementations.

As such, code-style isn't really consistent with the rest of the codebase, and these tests should NOT be touched once first implemented, to ensure minimal divergence from the original test suite.

This will subsequently ease eventual upstreaming of pyochain reimplementations to the original libraries, if desired.
"""
