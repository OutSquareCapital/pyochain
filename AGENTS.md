# Agents

Most repo informations are in the CONTRIBUTING file.

[contributing](./CONTRIBUTING.md)

When editing rust files, always run the following command to check if it compiles:

```shell
cargo fmt --all && uv run cargo clippy --workspace
```
